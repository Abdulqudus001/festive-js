document.addEventListener('load', () => {
  const transformPage = (() => {
    // Selects navbar with class of "festive-nav"(Assuming there is only one navbar 😉)
    const navbar = document.querySelector('.festive-nav');
  })();
});